//! Slice - slice(x, y);

const firstName = "Matt";
firstName.slice(0, 1); // Taking a 'slice' out of firstName starting at 0 and up to, not including, 1.
firstName.slice(0, 3) // 'Mat'
//? Do some basic subtraction to figure out how many characters you'll end up with.

//! ToUpperCase - string.toUpperCase()
let myName = "Matt"
upperCaseName = myName.toUpperCase();